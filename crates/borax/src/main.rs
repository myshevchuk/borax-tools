//! The `borax` CLI binary. Subcommands and pipeline wiring are
//! implemented per the `add-core-pipeline` OpenSpec change; the work
//! itself lives in the `borax` library beside this file.

fn main() {}
